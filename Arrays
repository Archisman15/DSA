//Find Largest Element in an Array
#include <bits/stdc++.h> 
int largestElement(vector<int> &arr, int n) {
    // Write your code here.
    int largest=arr[0];
    for(auto it:arr)
    {
        if(it>=largest)
            largest=it;
    }
    return largest;
}

